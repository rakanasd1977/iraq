{
  "name": "rafidain-market",
  "version": "1.0.0",
  "private": true,
  "description": "منصة سوق الرافدين - سوق إلكتروني عراقي يعمل في 18 محافظة (متاجر، مطاعم، فنادق، حجز طيران، مكاتب سفر)",
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix admin-panel && npm install --prefix agent-panel && npm install --prefix provider-panel && npm install --prefix customer-mobile",
    "seed": "npm run seed --prefix server",
    "migrate": "npm run migrate --prefix server",
    "server": "npm run dev --prefix server",
    "admin": "npm run dev --prefix admin-panel",
    "agent": "npm run dev --prefix agent-panel",
    "provider": "npm run dev --prefix provider-panel",
    "customer": "npm run dev --prefix customer-mobile",
    "build:admin": "npm run build --prefix admin-panel",
    "build:agent": "npm run build --prefix agent-panel",
    "build:provider": "npm run build --prefix provider-panel",
    "build:customer": "npm run build --prefix customer-mobile",
    "build": "npm run build:customer && npm run build:agent && npm run build:provider && npm run build:admin",
    "test": "npm run test --prefix server"
  }
}
